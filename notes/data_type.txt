numeric
    int = to enter interger value (1,2,3,4,5)
    float = enter desimal value(10.5, 10.7)
    complex = to enter mix value (1j+5, 5f-)
sequential datatype
    str = enter string(normal caracter)
    list = enter list start [10,20,30]
    tuple = enter ()
    range range(1,10)
map datatype
    dict {key:value}
set datatype
    set use{}
boolean datatype
    bool = enter true or false

function 
    type
    input
    print
    format
    split

concatination

Arithmetic Operators

Operator	Meaning	Example
+	Addition	10 + 5 = 15
-	Subtraction	10 - 5 = 5
*	Multiplication	10 * 5 = 50
/	Division	10 / 5 = 2.0
%	Modulus (remainder)	10 % 3 = 1
**	Exponent (power)	2 ** 3 = 8
//	Floor division	10 // 3 = 3

Comparison (Relational) Operators

Result is always True or False.
Operator	Meaning
==	Equal to
!=	Not equal to
>	Greater than
<	Less than
>=	Greater than or equal to
<=	Less than or equal to

Logical Operators

Used to combine conditions.

Operator	Meaning
and	True if both conditions are true
or	True if at least one is true
not	Reverses the result

Assignment Operators

Used to assign values.

Operator	Example	Same As
=	x = 5	Assign
+=	x += 5	x = x + 5
-=	x -= 5	x = x - 5
*=	x *= 5	x = x * 5
/=	x /= 5	x = x / 5

Membership Operators
Operator	Meaning
in	True if value exists
not in	True if value does not exist

Identity Operators
Operator	Meaning
is	True if same object
is not	True if not same object

statement
    if
    if else
    elif
    nested if

looping statement
    for
    while

control statement
    break
    continue
    pass


string
len(st)
st.lower()
st.casefold()
st.upper()
st.title()
st.capitalize()
st.strip()
st.replace()
st.find()
st.startwith()
st.endwith()
st.split()
join()
isalpha()
isdigit()
usalnum()
zfill()
center()

strin.slicing()

    